# HANDOFF — единый мастер-контекст Casas Fortunae

> **Это ЕДИНЫЙ файл для передачи проекта между сессиями (точка форка).**
> Чтобы продолжить в новом чате без потери контекста и качества — прочитай:
> 1. **этот файл** (HANDOFF.md) — рабочий контекст, история, что дальше;
> 2. **`CLAUDE.md`** — технический канон (механика, карта кода, баланс-патчи);
> 3. **`FEATURES.csv`** — каноническая таблица всех фич (user story + ожидаемое
>    поведение + статус).
>
> Больше плодить документы не нужно (не распыляться). Старый `PLAN_FOR_OPUS.md`
> удалён — пайплайн пройден. Обновляй ЭТОТ файл в конце каждой значимой итерации.
> Язык общения с пользователем — **русский**.

_Последнее обновление: визуальный редизайн Фаза 1 (вход + система ink-анимации)._

---

## 1. Где лежит проект и как с ним работать

- **Репозиторий:** `arniByte/CasasFortunae` (GitHub). Доступ — через GitHub MCP
  (`mcp__github__*`), а также обычный `git` (push работает, установлен Claude GitHub App).
- **Основная ветка:** `main`. Рабочая ветка для разработки: `claude/vibrant-bohr-z6fpmp`.
  После мержа PR ветку сбрасываем на свежий `main` (`git checkout -B claude/vibrant-bohr-z6fpmp origin/main`).
- **Workflow:** разработка на ветке → коммит (подписанный, `-S`) → push `-u origin <branch>`
  → создать **draft PR** → снять с draft → squash-merge в main → синхронизировать локальный main.
- **Коммиты подписаны** SSH-ключом (gpgsig). В конце commit message:
  `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>` + `Claude-Session: ...`.
- **CI в репо нет** (0 workflows) — проверки гоняем локально вручную.
- **Зависимости** ставятся SessionStart-хуком (`.claude/hooks/session-start.sh`)
  в web-сессиях: `pip install --ignore-installed blinker -r requirements.txt`.

### Песочница — ограничения
- **Нет интернета к Met API (403)** — аватары/фоны падают на SVG/градиентные заглушки.
  На машине пользователя грузятся. В тестах это НЕ баг.
- **Playwright недоступен** (нет браузеров) — UI-тесты гоняем альтернативно:
  Flask `test_client().get('/')` (рендер 200 + наличие нужных строк) и прямые
  юнит-проверки `GameState` из Python.

### Проверки перед сдачей (обязательно)
```bash
python3 -c "import ast; ast.parse(open('server.py').read())"      # синтаксис
grep -c '<style' templates/game.html   # 3,  </style> 3
grep -c '<script>' templates/game.html # 1,  </script> 1
python3 simulate.py --games 200 --seed 42   # регрессия баланса (до/после правок)
python3 -c "import server; c=server.app.test_client(); print(c.get('/').status_code)"  # 200
```

---

## 2. Что уже сделано (хронология этой работы)

1. **Инфраструктура репо:** разложена структура игры, настроен self-managed git/PR-флоу,
   SessionStart-хук для зависимостей, `.gitignore`.
2. **Направление 1 — симулятор баланса** (`simulate.py`, отчёт `SIM_REPORT.md`).
   Headless-прогон реального `GameState` с 4 ботами (aggressor/economist/random/adaptive).
   Детерминирован по `--seed`. См. CLAUDE.md §2.
3. **Направление 2 — баланс Домов** (PR #2, смержен). Нёрф Сфорца (+1 броня/ход,
   pike↑, riposte↓, cannon cap), бафф Медичи (+2 флорин, banco, commission+draw, bribe+dmg),
   ускорение Борджиа (apple cost↓), усиление Эсте (surge pierce, harmonic↓, Муза+heal,
   Астролябия cleanse+florin). Подробности — CLAUDE.md §13b.
   Итог: Сфорца 79.5→70%, Эсте 39→50.7% ✅, Борджиа→Сфорца контр 32→60% ✅.
4. **Направление 3 — индикатор угрозы** (PR #3, смержен). Бейдж «под угрозой»
   + серверная оценка летальности. CLAUDE.md §13c. Баланс не затронут.
5. **Направление 3 — лимит карт за ход** (`CARDS_PER_TURN=3`, PR #4 смержен).
   CLAUDE.md §13d. Баланс Домов почти не сдвинулся; fatigue вырос 54→60%.
6. **Направление 4 — Сострадание Фортуны** (`COMPASSION_THRESHOLD=10`, PR #5 смержен).
   CLAUDE.md §13e. Мягкий комебэк: при престиже ≤10 Колесо благословляет самую частую
   стихию руки. Данные: игра уже не снежит (56%) → выбран мягкий вариант. 33% партий.
7. **Направление 5 — 5-й Дом Савонарола** (ниша жертвы/самопожертвования). CLAUDE.md §13f.
   Дебют сразу сбалансирован: 50.7% (зеркало 49%). 6 новых карт + 6 новых kind, пассивка
   «Праведный гнев» (+2 урона пока отстаёшь), реликвия «Власяница». Симулятор обучен
   новым kind (защита от суицида ботов). Сфорца/Борджиа просели к коридору как побочка.
8. **Аудит фич + баланс Медичи + UX-фиксы** (§13g). `FEATURES.csv` (75 фич, user story +
   ожидаемое поведение + статус). Win-condition Медичи `condotta` (флорины→урон):
   15.7%→22%. Фиксы: реванш не возвращал 2-го игрока (BUG-B), рассинхрон готовности
   при смене Дома (BUG-A), guard самопоражения жертвенными картами. Удалён PLAN_FOR_OPUS.md.

### Текущие винрейты (симулятор, 5 Домов, ~500 партий/пара, seed 11)
| Дом | Винрейт | Статус |
|-----|---------|--------|
| Сфорца | ~68% | высок, но в основном из-за разгрома бото-слабого Медичи и контра Эсте (91%, дизайн); равные матчапы в норме — не нёрфить на ботах |
| Борджиа | ~63% | DoT-ниша сильна по дизайну; равные матчапы ок |
| Эсте | ~50% ✅ | в коридоре |
| Савонарола | ~48-50% ✅ | в коридоре |
| Медичи | ~22% | win-condition добавлен; остаток — слепое пятно ботов (тонкий темп); в живой игре сильнее |

### Что дальше
- **ИДЁТ — ВИЗУАЛЬНЫЙ РЕДИЗАЙН** (см. раздел «Редизайн» ниже). Фаза 1 готова.
- Играбельный ИИ-бот для одиночки (adaptive из simulate.py довести до уровня).
- Артефакты по Дому / больше карт; умнее бот → честнее оценка Медичи.

---

## РЕДИЗАЙН — «Живой иллюминированный манускрипт»

**Концепция:** вся игра выглядит как старинный рукописный кодекс, который *рисуется
чернилами на глазах*. Каждый экран/карта/герб — рукотворная гравюра; элементы
проявляются анимацией натекающих чернил; переходы плавные и тактильные.

**Опорные ассеты** (векторизованы из рефов пользователя через potrace, чернила `#241910`):
- `static/art/eagle.svg` (98 KB) — геральдический орёл (эмблема/герб/загрузка).
- `static/art/knight.svg` (341 KB) — конный рыцарь (герой/фон).
- Источник рефов: `art.zip` в корне (положил пользователь). Пайплайн векторизации —
  в `/tmp/art/*.py` (НЕ в репо; окружение эфемерно — переустановить
  `potrace`/`cairosvg`/`numpy`/`scipy`/`playwright` в новой сессии).

**Инструмент превью:** Playwright + chromium РАБОТАЮТ в этой песочнице.
`/tmp/shot.py` снимает живой скриншот (запусти `python server.py` в фоне, затем
скрипт; `localStorage cf_tut=1` чтобы не лез туториал). Met/шрифты дают сетевые
ERR_CERT — это НЕ JS-ошибки; смотри счётчик `pageerror` (должен быть 0).

**Система ink-анимации (в `game.html`):**
- Фильтры `#inkRough` (живая рукописная кромка с анимацией seed), `#inkBleed` (растекание).
- `loadInkArt(name)` — fetch+кэш SVG из `/static/art/`; `window.ART` кэш.
- `inkDrawPath(path,dur,delay)` — самопрорисовка штриха (stroke-dashoffset).
- CSS `.ink-reveal.drawn` — clip-wipe снизу-вверх + bloom (для залитого арта).
- `.entry-rise` — каскадное появление; `.dropcap` — буквица.

**Статус фаз** (трекинг — `FEATURES.csv`, строки F76-F81):
- **Фаза 1 — Вход + фундамент: ГОТОВО** ✅. Орёл-герой проявляется чернилами,
  самопрорисованная рамка-кодекс, буквица «C», каскад текста. 0 JS-ошибок.
- Фаза 2 — Выбор Дома: TODO (гербы в гравюрном стиле, ink-печать выбора).
- Фаза 3 — Поле боя: TODO (ink-рамки полос, Колесо, чернильные росчерки переходов).
- Фаза 4 — Карты + полировка: TODO (лицо карт, hover/play, splash в синхрон со звуком).

**Канон при редизайне:** тёплая палитра, синего нет; новый SVG — `stroke-linejoin/
linecap="round"` + ink-фильтр; чернила `#241910`, не чистый чёрный; постоянные
элементы вне `#stage`; проверять теги (`<style>`3/3,`<script>`1/1) и 0 pageerror.

### (архив) Винрейты до этого этапа
| Дом | Винрейт | Статус |
|-----|---------|--------|
| Сфорца | ~70% | Сфорца→Эсте 89% — дизайн-контр |
| Борджиа | ~66% | ок (DoT-ниша сильна по дизайну) |
| Эсте | ~50% | ✅ в коридоре |
| Медичи | ~13% | занижен ботами (нет прямого урона; в живой игре сильнее) |

---

## 3. Что дальше по пайплайну (приоритет сверху вниз)

- **Направление 3 (продолжение, опц.):** лимит карт за ход (≤3) — меняет темп,
  ТРЕБУЕТ обсуждения с пользователем (вкус) + симуляции. НЕ кодить молча.
- **Направление 4 — comeback-механика.** Варианты в PLAN (Сострадание Фортуны и т.п.).
  ВЫБОР ВКУСА — предложить пользователю цифры из симулятора, не решать самому.
- **Направление 5 — контент:** 5-й Дом (ниши свободны: манипуляция рукой, жертва/обмен,
  воскрешение из сброса), по артефакту на Дом, играбельный ИИ-бот из adaptive.
- **Доводка баланса Медичи/Сфорца** при появлении данных получше (умнее боты).

### ⏳ ОТЛОЖЕННАЯ ЗАДАЧА — визуал (ждёт пользователя)
Пользователь хочет ручную отрисовку как на ч/б референсе (геральдический орёл на
знамени с кистями + конный рыцарь). Решение: **векторизовать реальный референс**
(не ИИ-SVG). Пайплайн готов:
- `potrace` 1.16 (apt) + `vtracer` (pip) + `cairosvg` (pip) — установлены в песочнице
  (НО переустанавливать в новой сессии, окружение эфемерно).
- Скрипт `/tmp/art/trace.py` (НЕ в репо): PIL preprocess → potrace → перекраска
  в чернила `#241910` → PNG-превью на пергаменте.
- **Блокер:** нужен файл-референс в репо. Просили загрузить в `art/refs/reference.png`
  (папка `art/refs/` создана, пустая). Как появится — `git pull`, прогнать трассировку,
  показать результат, затем вставить в: гербы Домов (`HOUSE_CREST`), экран входа
  (`#entry`), иллюстрации карт (`CARD_ART`/`cardArtSVG`), баннер хода.

---

## 4. Ключевые места в коде (быстрый указатель)

**server.py (~1080 строк):**
- Константы: `START_PRESTIGE=30`, `START_HAND=4`, `FLORIN_CAP=10`, `MAX_FLORINS=15`, `MAX_ARMOR=20`.
- `HOUSES` (4 Дома), `CATALOGUE` (43 карты через `C()`), `build_house_deck()`.
- `PlayerState`, `GameState` (см. CLAUDE.md §7). Угроза: `_est_damage`/`_lethal_threat`.
- `view_for(seat)` — сериализация; рука соперника вырезается; есть `under_threat`.
- Flask: `/`, `/api/{houses,avatars,music,backgrounds}`, `/static/music/<f>`, WS `/ws`.

**templates/game.html (~1150 строк):** один файл, CSS+JS инлайн. JS-объекты
`Music/Backgrounds/SFX/Tutorial`. Рендер: `buildBoard`/`updateBoard`/`renderHand`.
Угроза: `threatHTML()`, CSS `.threat-*`, диф `prevThreat`. SVG-арт: `CARD_ART`,
`wheelSVG`, `laurelSVG`, `HOUSE_CREST`, `svgAvatar`.

**simulate.py:** боты + `card_pressure`/`_lethal_card`/`house_matrix`/`strategy_matrix`.
Патчит `secrets` на seeded RNG для детерминизма.

---

## 5. Каноны, которые НЕЛЬЗЯ нарушать
- **Палитра тёплая, СИНЕГО НЕТ** (CLAUDE.md §9). Легаси-алиасы синего перенаправлены.
- **Рисованный ink-стиль:** новый SVG — со `stroke-linejoin/linecap="round"` +
  `filter="url(#inkS)"`. Чернила `#241910` на пергаменте, не чистый чёрный.
- **Постоянные элементы** (`<audio>`, `#bgGallery`, `#musicDock`, `#tutorial`) — вне `#stage`.
- **Текст карт = реальный эффект** (был баг surge — текст врал). Проверять при правках.
- **mp3** — личные записи пользователя; в переиспользуемый скилл НЕ класть (в игровом
  репо они нужны и уже лежат).
- **Любая правка чисел карт** → симулятор до/после + показать разницу цифрами.
- **Механики «на вкус» (Напр. 3-4)** → согласовать с пользователем перед кодом.
